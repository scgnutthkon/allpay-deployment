apiVersion: v1
kind: Service
metadata:
  name: {{ include "allpay-web.fullname" . }}
  labels:
    {{- include "allpay-web.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "allpay-web.selectorLabels" . | nindent 4 }}
