apiVersion: v1
kind: Service
metadata:
  name: {{ include "allpay-webapi.fullname" . }}
  labels:
    {{- include "allpay-webapi.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "allpay-webapi.selectorLabels" . | nindent 4 }}
