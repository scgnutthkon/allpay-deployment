1. Get the application running result these commands:
  export NODE_PORT=$(kubectl get pods --selector=job-name={{ .Chart.Name }} --namespace {{ .Release.Namespace }} --output=jsonpath='{.items[*].metadata.name}')
  kubectl logs $NODE_PORT --namespace {{ .Release.Namespace }}