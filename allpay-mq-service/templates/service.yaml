apiVersion: v1
kind: Service
metadata:
  name: {{ include "allpay-mq-service.fullname" . }}
  labels:
    {{- include "allpay-mq-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "allpay-mq-service.selectorLabels" . | nindent 4 }}
