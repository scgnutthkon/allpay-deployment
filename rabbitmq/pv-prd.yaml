apiVersion: v1
kind: PersistentVolume
metadata:
  name: allpay-rabbitmq
spec:
  capacity:
    storage: 10Gi
  accessModes:
    - ReadWriteOnce
  storageClassName: "allpay-rabbitmq-storage"
  nfs:
    path: /var/nfs/allpay-rabbitmq
    server: 10.100.12.94