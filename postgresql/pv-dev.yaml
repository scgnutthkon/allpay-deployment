apiVersion: v1
kind: PersistentVolume
metadata:
  name: allpay-postgresql
spec:
  capacity:
    storage: 20Gi
  accessModes:
    - ReadWriteOnce
  storageClassName: "allpay-postgresql-storage"
  nfs:
    path: /var/nfs/allpay-postgresql
    server: 10.100.12.94