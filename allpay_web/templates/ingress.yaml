apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: allpay-ingress
  namespace: allpay
spec:
  rules:
  - http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: allpay-web-service
            port:
              number: 80
