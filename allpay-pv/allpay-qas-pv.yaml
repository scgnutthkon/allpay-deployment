apiVersion: v1
kind: PersistentVolume
metadata:
  name: allpay-qas-storage
spec:
  capacity:
    storage: 50Gi
  accessModes:
    - ReadWriteMany
  storageClassName: "allpay-qas-storage"
  nfs:
    path: /var/nfs/allpay-qas-files
    server: 10.100.12.94